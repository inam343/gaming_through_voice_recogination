<Window x:Class="GamingThroughVoiceRecognitionSystem.Views.HomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Home - Gaming Through Voice"
        Height="900" Width="1600"
        MinHeight="720" MinWidth="1200"
        WindowStyle="None"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <!-- Hoverable Card Style -->
        <Style x:Key="HoverableCardStyle" TargetType="Border">
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="2" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1.05" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1.05" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="Effect.(DropShadowEffect.Opacity)" To="0.35" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="Effect.(DropShadowEffect.Opacity)" To="0.2" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Gaming Button Style -->
        <Style x:Key="GlassyButton" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="Background" Value="{DynamicResource GlassBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource GlassBorderBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="10"
                                SnapsToDevicePixels="True">
                            <Border.Effect>
                                <DropShadowEffect x:Name="glow" Color="{StaticResource PrimaryGlowColor}" BlurRadius="0" ShadowDepth="0" Opacity="0"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="12,0,0,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" 
                                                          To="#40FFFFFF" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="glow" Storyboard.TargetProperty="BlurRadius" To="15" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="glow" Storyboard.TargetProperty="Opacity" To="0.6" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="glow" Storyboard.TargetProperty="BlurRadius" To="0" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="glow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#60FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Dynamic Gaming Background -->
        <Rectangle Fill="{DynamicResource BackgroundGradientBrush}"/>

        <!-- Main Layout Grid -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Gaming Sidebar -->
            <Border Grid.Column="0" Background="{DynamicResource SidebarBackgroundBrush}" 
                    BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="0,0,1,0" Padding="24">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2" Opacity="0.08"/>
                </Border.Effect>
                <StackPanel>
                    <!-- Gaming Logo -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,48">
                        <Border Width="48" Height="48" CornerRadius="12" Margin="0,0,12,0">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="{DynamicResource PrimaryColor}" Offset="0"/>
                                    <GradientStop Color="{DynamicResource SecondaryColor}" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect Color="{StaticResource PrimaryGlowColor}" BlurRadius="15" ShadowDepth="0" Opacity="0.4"/>
                            </Border.Effect>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="VOICE GAMING" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚡" FontSize="10" Margin="0,0,4,0"/>
                                <TextBlock Text="AI POWERED" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}" FontWeight="SemiBold"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>

                    <!-- Navigation Buttons -->
                    <StackPanel>
                        <Button x:Name="HomeButton" Style="{StaticResource GlassyButton}" Margin="0,0,0,8" Height="40" Click="HomeButton_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🏠" Margin="0,0,12,0"/>
                                <TextBlock Text="Home"/>
                            </StackPanel>
                        </Button>

                        <Button x:Name="VoiceCommandsButton" Style="{StaticResource GlassyButton}" Margin="0,0,0,8" Height="40" Click="VoiceCommandsButton_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🎤" Margin="0,0,12,0"/>
                                <TextBlock Text="Voice Commands"/>
                            </StackPanel>
                        </Button>

                        <Button x:Name="ProfileButton" Style="{StaticResource GlassyButton}" Margin="0,0,0,8" Height="40" Click="ProfileButton_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="👤" Margin="0,0,12,0"/>
                                <TextBlock Text="My Profile"/>
                            </StackPanel>
                        </Button>

                        <Button x:Name="SettingsButton" Style="{StaticResource GlassyButton}" Margin="0,0,0,8" Height="40" Click="SettingsButton_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚙️" Margin="0,0,12,0"/>
                                <TextBlock Text="Settings"/>
                            </StackPanel>
                        </Button>

                        <Button x:Name="LogoutButton" Style="{StaticResource GlassyButton}" Margin="0,32,0,0" Foreground="#EF4444" Height="40" Click="LogoutButton_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⏻" Margin="0,0,12,0"/>
                                <TextBlock Text="Logout"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Gaming Top Bar -->
                <Border Grid.Row="0" Background="{DynamicResource GlassBackgroundBrush}" 
                        BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="0,0,0,1" Padding="16,0">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="1" Opacity="0.06"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- User Info -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,24,0">
                            <StackPanel VerticalAlignment="Center" Margin="0,0,16,0">
                                <TextBlock x:Name="UserNameText" Text="User Name" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="14" FontWeight="Bold"/>
                                <TextBlock Text="⭐ Premium Gamer" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" FontWeight="SemiBold"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Window Controls -->
                        <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Width="36" Height="28" Click="MinimizeButton_Click" Background="Transparent" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" Margin="4,0" Cursor="Hand">
                                <TextBlock Text="─" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="Bold"/>
                            </Button>
                            <Button Width="36" Height="28" Click="MaximizeButton_Click" Background="Transparent" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" Margin="4,0" Cursor="Hand">
                                <TextBlock Text="☐" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="Bold"/>
                            </Button>
                            <Button Width="36" Height="28" Click="CloseButton_Click" Background="Transparent" BorderBrush="#FF4444" BorderThickness="1" Margin="4,0" Cursor="Hand">
                                <TextBlock Text="✕" Foreground="#FF4444" FontWeight="Bold"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Content Area - Dynamic Content Container -->
                <ContentControl x:Name="MainContentArea" Grid.Row="1"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
